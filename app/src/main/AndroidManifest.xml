<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".MiMascotaApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo1_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo1_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiMascota"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MiMascota">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.ProductoListaActivity"
            android:exported="false"
            android:label="@string/productos"
            android:theme="@style/Theme.MiMascota" />
        <activity
            android:name=".ui.activity.CheckoutActivity"
            android:exported="false"
            android:label="Checkout"
            android:theme="@style/Theme.MiMascota.MaterialComponents" />
        <activity
            android:name=".ui.activity.OrdenExitosaActivity"
            android:exported="false"
            android:label="Orden Exitosa"
            android:theme="@style/Theme.MiMascota" />
        <activity
            android:name=".ui.activity.MisPedidosActivity"
            android:exported="false"
            android:label="Mis Pedidos"
            android:theme="@style/Theme.MiMascota" />
    </application>

</manifest>