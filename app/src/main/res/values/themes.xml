<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MiMascota" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Tema para Bottom Sheet -->
    <style name="BottomSheetDialogTheme" parent="Theme.Material3.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>

    <style name="BottomSheetStyle" parent="Widget.Material3.BottomSheet">
        <item name="shapeAppearanceOverlay">@style/BottomSheetShapeAppearance</item>
    </style>

    <style name="BottomSheetShapeAppearance" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">16dp</item>
        <item name="cornerSizeTopRight">16dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>

    <!-- Tema basado en MaterialComponents para Activities que usan vistas de Material (AppBarLayout, MaterialToolbar, TextInputLayout, etc.) -->
    <style name="Theme.MiMascota.MaterialComponents" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Mantener colores heredados o personalizar si se desea -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">@color/primary</item>
    </style>

</resources>